<!DOCTYPE html>
  <%- include ('./partials/head.ejs') %>
  <body>
    <header><%- include ('./partials/nav.ejs') %></header>
    <main>
      <h2>All Ress</h2>
      <% if (ress.length > 0) { %> <% ress.forEach(ress => { %>
        <article>
          <header>
            <h3><%= ress.title %></h3>
          </header>
          <p><%= ress.snippet %></p>
          <footer>
            <a href="/blog/<%= ress.id %>"> Read more 👉🏾</a>
          </footer>
        </article>
      <% }) %> <% } else { %>
      <h1>You have no Ress</h1>
      <% }%>
    </main>
    <%- include('./partials/footer.ejs') %>
  </body>
</html>
