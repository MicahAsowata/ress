<!DOCTYPE html>
<html lang="en">
  <%- include ('./partials/head.ejs') %>
  <body>
    <header><%- include ('./partials/nav.ejs') %></header>
    <main>
      <h2>All Ress</h2>
      <% if (ress.length > 0) { %> <% ress.forEach(ress => { %>
      <h3><%= ress.title %></h3>
      <p><%= ress.body %></p>
      <% }) %> <% } else { %>
      <h1>You have no Ress</h1>
      <% }%>
    </main>
  </body>
</html>
